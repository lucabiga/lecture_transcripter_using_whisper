# IMPORTANT: Install PyTorch with CUDA support FIRST, then install other packages
# For CUDA 12.x (recommended for NVIDIA RTX 30xx, 40xx series):
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# For CUDA 11.8:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# For CPU-only (NOT recommended, very slow):
# pip install torch torchvision torchaudio

# After installing PyTorch, install these:
openai-whisper
tqdm

# For API Server (optional - only if using remote transcription service):
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
aiofiles>=23.0.0